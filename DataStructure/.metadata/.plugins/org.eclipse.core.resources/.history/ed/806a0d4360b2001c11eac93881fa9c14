package CircularQueue;
public class CMain {

	public static void main(String[] args) {

			CQueue q1=new CQueue();
			CQ.insert(10);
			CQ.insert(20);
			CQ.insert(30);
			//CQ.remove();
			//CQ.remove();
			CQ.insert(40);
			CQ.insert(50);
			CQ.insert(60);
			CQ.remove();
			CQ.remove();
			
		System.out.println(q1);
	}
	
	
	
}
